# Base URL of the PartyKit party server (scheme optional, no trailing slash).
# Local dev: leave as localhost:1999 (the `npx partykit dev` default port).
# Production: set to your deployed party URL, e.g. ypad-<username>.partykit.dev
VITE_PARTYKIT_HOST=localhost:1999