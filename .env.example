# Host of the Yjs sync worker (scheme optional, no trailing slash).
# Local dev: leave as localhost:8787 (the `npx wrangler dev` default port).
# Production: set to your deployed worker, e.g. ypad.<username>.workers.dev
VITE_SYNC_HOST=localhost:8787